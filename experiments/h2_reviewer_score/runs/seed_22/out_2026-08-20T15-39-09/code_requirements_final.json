["Use OmniRec only for dataset loading, preprocessing, splitting, algorithm execution, and evaluation; do not invoke other recommender backends directly.", "Load the MovieLens100K dataset via OmniRec's dataset API.", "Apply OmniRec's UserHoldout split with random seed 22, and keep the split reproducible.", "Run exactly two algorithms: ItemKNN and a popularity baseline, both configured through OmniRec.", "Evaluate both algorithms on the held-out test set with NDCG@10 and Precision@10.", "Use a top-k ranking evaluation setup compatible with the MovieLens100K experiment.", "Preserve the existing script structure and outputs; extend it incrementally rather than rewriting it.", "Return a final results table that lists both algorithms and their NDCG@10 and Precision@10 values side by side.", "Keep all random behavior controlled by the specified seed so repeated runs are identical."]