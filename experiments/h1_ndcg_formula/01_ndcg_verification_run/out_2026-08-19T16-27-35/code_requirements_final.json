["Use OmniRec as the only framework for loading data, preprocessing, experiment planning, running, and evaluation.", "Load MovieLens100K via RecSysDataSet.use_dataloader(DataSet.MovieLens100K).", "Apply a UserHoldout split with train/validation/test held out through OmniRec preprocessing.", "Convert ratings to implicit feedback before fitting ItemKNN.", "Configure exactly one required algorithm: LensKit.ItemKNNScorer.", "Evaluate with OmniRec ranking metrics and report NDCG@10 on the held-out test split.", "Set OmniRec's random state before preprocessing or training so the split and results are reproducible.", "Run the experiment end-to-end through run_omnirec so training, prediction, and evaluation are handled by the framework.", "Preserve the existing script structure and outputs, extending it incrementally rather than rewriting the script from scratch.", "Do not add extra algorithms, datasets, metrics, or plots unless they are already required by the existing prototype for correctness."]